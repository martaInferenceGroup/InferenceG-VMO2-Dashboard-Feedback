# Server
PORT=3000
BASE_URL=http://localhost:3000

# Authentication (change these before deploying!)
CLIENT_PASSWORD=client-review-2026
REVIEWER_PASSWORD=reviewer-admin-2026

# Email notifications (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
NOTIFY_EMAIL_TO=marta@example.com
NOTIFY_EMAIL_FROM=VMO2 Dashboards <noreply@example.com>

# Webhook notifications (Teams/Slack)
NOTIFY_WEBHOOK=

# Claude Code CLI path (if not in PATH)
CLAUDE_CMD=claude
